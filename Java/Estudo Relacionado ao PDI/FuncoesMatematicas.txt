Funcoes matemáticas em java

public class Main{
	public statis void main(String[] args){
	
	double valor;
	double x = 3,0;
	double a = 4,0;
	double d = -5,0;
	
	
	valor = Math.sqrt(x);   // Calcula a raiz quadrada de x
	valor = Math.pow(x, a); // Faz a Potenciacao de x por a
	valor = Main.abs(d);	// Mostra o valor absulo de x sem o valor negativo
	
	// é possivel montar expressoes matemáticas complexas
	// formual de baskara
	
	double delta;
	double x1,x2;
	
	delta = Math.pow(b , 2.0) - 4*a*c;
	
	x1 = (-b + Math.sqrt(delta)) / (2,0 a);
	x1 = (-b - Math.sqrt(delta)) / (2,0 a);
	
	
		
	}
}